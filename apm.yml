name: apm-loadout
version: 1.0.0
description: APM skill installation and conversion toolkit

# Install: apm install samcrock/apm-loadout
# Or: git clone samcrock/apm-loadout && cd apm-loadout && apm install

dependencies:
  apm:
    # Productivity
    - github.com/samcrock/apm-loadout/.apm/skills/productivity/apm-converter