name: cc-loadout
version: 1.0.0
description: Claude Code skill suite

# Install: apm install samcrock/cc-loadout
# Or: git clone samcrock/cc-loadout && cd cc-loadout && apm install

dependencies:
  apm:
    # Productivity
    - github.com/samcrock/cc-loadout/.apm/skills/productivity/apm-converter